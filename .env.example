# 百度 API 配置
# 可以配置多个 key 进行轮询，格式为 JSON 数组
BAIDU_KEYS=[{"client_id":"your_client_id_1","client_secret":"your_client_secret_1"},{"client_id":"your_client_id_2","client_secret":"your_client_secret_2"}]

# Redis 配置
# 基本格式：redis://host:port/db
# 带密码格式：redis://:password@host:port/db
REDIS_URL=redis://localhost:6379/8

# 可选：如果 REDIS_URL 中没有包含密码，可以单独设置
# REDIS_PASSWORD=your_redis_password

# API密钥校验 - 防止盗刷（可选，不设置则不校验）
API_KEY=admin

# 可选：覆盖百度 API 地址
# BAIDU_TOKEN_URL=https://aip.baidubce.com/oauth/2.0/token
# BAIDU_OCR_URL=https://aip.baidubce.com/rest/2.0/ocr/v1/multiple_invoice

# Token 配置
# 百度API限制：30天有效期，1000次/月，2QPS
# 优化策略：主要依赖TTL，使用次数作为保险机制
TOKEN_MAX_USES=900

# 健康检查配置
MAX_CONSECUTIVE_ERRORS=3
HEALTH_CHECK_INTERVAL=3600


# 配额监控配置
MONTHLY_QUOTA_LIMIT=1000
QPS_LIMIT=2